# Wave 0 — substrate

**Status:** complete

**Built:**
- Dependencies installed across all three workspaces (Kysely, pg, Express, node-pg-migrate, kysely-codegen, Vitest, React/Vite), plus root `tsconfig.json` and workspace scripts.
- Migrations `0001`–`0007`: 16 tables, 4 enums, 15 CHECKs, 7 UNIQUEs (2 `DEFERRABLE INITIALLY DEFERRED`), 21 FKs all `ON DELETE RESTRICT`, 12 partial indexes. Phase-1 tables only per D3.
- `server/src/db/index.ts` — Kysely instance + pool sized to the operating envelope, not to load.
- `server/src/db/transaction.ts` — `writeTransaction()`: `SERIALIZABLE`, retry on `40001` bounded at 3, structured retry logging.
- `server/src/db/types.ts` — generated by `kysely-codegen` from the live database.
- `server/test/fixtures.ts` — `makeUser/makeDriver/makeAdmin/makeDonor/makeTruck/makeRoute/makeShift/makeClaimedShift/makeAvailabilityBlock/resetDatabase`.
- `scripts/test-db.sh` — rewritten off Docker onto local Postgres 16, one database per worktree.
- `scripts/gate.sh` — the mechanical gate, 5 checks, exit code is the verdict.

**Files:** `server/migrations/000{1..7}_*.sql`, `server/src/db/{index,transaction,types}.ts`, `server/test/{fixtures.ts,substrate.test.ts}`, `server/vitest.config.ts`, `scripts/{test-db,gate}.sh`, `tsconfig.json`, `{,server/,client/}package.json`, `docs/features/phase-1-{build-plan,state}.md`, `docs/foundation/{architecture,data-model,product-requirement}.md`, `.worktreeinclude`, `.claude/agents/wave-lane.md`.

**Invariants:** enforced at tier 1 and negative-tested — I1/I2 (tier enum, duty junction), I3 (`ck_user_username`, `uq_user_username` spanning deactivated users), I4, I7 (`ck_shift_owner`), I8 (`ck_shift_truck`), I19, I21 (FK-`RESTRICT` as the history guard), I22 (no truck exclusivity), I26, I28 (`uq_route_stop_donor`, `uq_shift_stop_donor`). Tier 2's substrate (`writeTransaction`) is in place; no tier-3 service exists yet.

**Tests:** 31 passing / 0 failing — `./scripts/gate.sh`.

**Deliberately not done:**
- `data-model.md §7` intake tables, the `donation_status` enum, and `ix_weight_active` / `ix_ud_confirmed` — deferred to Phase 2 per D3.
- No services, routes, middleware, jobs, or client code. That is Waves 1–4.
- No `session` service logic; the table exists, the auth flow is Wave 1's.

**Assumed:**
- **A3 (open, non-blocking):** the four `app_config` session-lifetime column names. `architecture.md §4.2` fixes the values (30 min / 12 h / 30 d / 7 d) and says they live in `app_config`, but names no keys. Naming is ours.
- A1 and A4 were raised as assumptions, escalated as HALTs, and resolved by human decision — see `phase-1-state.md`. A2 was resolved against `ui-ux-spec.md S1.8`.

**Unblocked:** every Wave-1 lane. Identity/auth has `app_user`, `user_duty`, `session`, `device`; the notification outbox has `notification` + `push_subscription`; the client lane never depended on this. `makeClaimedShift()` unblocks the Wave-3 execution lane ahead of schedule.
