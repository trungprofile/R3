// S1.1 Login. The lead wires this into `main.tsx`'s SCREENS registry as `login`;
// the route it answers to is already declared in `app/routes.ts`.

export { LoginScreen } from './LoginScreen.tsx';
export { COPY, LOCKED_TEXT, MAX_TRIES } from './login.ts';
